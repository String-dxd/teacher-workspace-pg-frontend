pre-commit:
  parallel: true
  commands:
    lint:
      glob: '*.{ts,tsx}'
      run: pnpm oxlint {staged_files}
    format:
      glob: '*.{ts,tsx,css,json,md}'
      run: pnpm prettier --check {staged_files}
    typecheck:
      run: pnpm tsc --noEmit
